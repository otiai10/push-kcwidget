# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index
POST    /queue/add                              QueueController.Add
POST    /user/register                          UserController.Register
GET     /user/:twitterIdStr                     UserController.Get
